"""Skills package.

Task 10 only needs the `SKILLS` dict keyed by slug — Task 11 will flesh
out per-skill system prompts and the query/deep-research skills. For now
we re-export the registry from `base.py` so callers can `from
backend.skills.base import SKILLS` as written in the plan.
"""
